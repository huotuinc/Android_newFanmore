<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="@dimen/mark_margin_size"
    tools:context=".AllScoreActivity" >

    <TextView
        android:id="@+id/txt"
        android:layout_width="@dimen/mark_bg_size"
        android:layout_height="@dimen/mark_bg_size"
        android:layout_centerInParent="true"
        android:background="@drawable/mark_gray_bg"
        android:text="@string/img"
        android:gravity="center"
        android:textColor="@color/gray_94"
        android:textSize="@dimen/checkin_text_size" />

</RelativeLayout>